name: flutter_utils_smartid_bam
description: A new flutter plugin project.
version: 1.1.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.example.flutter_utils_smartid_bam
        pluginClass: FlutterUtilsSmartidBamPlugin
      ios:
        pluginClass: FlutterUtilsSmartidBamPlugin